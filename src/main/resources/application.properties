# Nom de l'application correspondant au fichier dans campushub-cloud-config
spring.application.name=campushub-scheduling-service

# Importation de la configuration depuis le Config Server
spring.config.import=optional:configserver:http://campushub-config:8888

# Profil par défaut
spring.profiles.active=default
